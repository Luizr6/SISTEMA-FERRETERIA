<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permisos de red para comunicar con la API REST de Django -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FerreteriaMovil"
        android:usesCleartextTraffic="true">

        <!-- Pantalla de Login como punto de entrada (LAUNCHER) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla de Catálogo de Productos e Inventario -->
        <activity
            android:name=".ui.catalogo.CatalogoActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustPan" />

        <!-- Pantalla de Detalle / Creación / Edición de Producto (CRUD) -->
        <activity
            android:name=".ui.detalle.DetalleProductoActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>
